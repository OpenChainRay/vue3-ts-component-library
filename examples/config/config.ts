// @ts-nocheck
// 自定义配置，参考 ./default/setting.config.js，需要自定义的属性在这里配置即可
const config = {
  theme: {
    color: '#00508f',
    // color: '#3370ff',
    mode: 'light',
  },
  animate: {
    name: 'fade',
    direction: 'left',
    disabled: true
  }
}

export default config
